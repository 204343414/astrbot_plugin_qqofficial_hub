{
  "callback_ttl_hours": {
    "description": "后台回调按钮有效期（小时）",
    "hint": "卡片保存新版本后，旧版本后台按钮立即失效；到期按钮返回重复/失效状态。",
    "type": "int",
    "default": 24
  },
  "image_host_enabled": {
    "description": "开启内置图床，让卡片能内嵌图片（棋盘、钢琴图）。需要配合 Cloudflare Tunnel 等把本地端口暴露到公网。",
    "type": "bool",
    "default": false
  },
  "image_host_base_url": {
    "description": "图床的公网地址，如 https://img.example.com（隧道指向本机 image_host_port）。留空则图床不可用。",
    "type": "string",
    "default": ""
  },
  "image_host_port": {
    "description": "图床本地监听端口，默认 9527。隧道 ingress 指到这个端口。",
    "type": "int",
    "default": 9527
  },
  "image_host_grace_seconds": {
    "description": "同一个群发出新图后，旧图再保留多久（秒），默认 300。QQ 是在用户滑到消息时才抓图，立刻删会让人看到裂图。",
    "type": "int",
    "default": 300
  },
  "recall_superseded_cards": {
    "description": "同一会话发出新卡片/新棋盘后，自动撤回它替代的那一条，避免长对局刷屏。QQ 仅允许撤回 2 分钟内的消息，超时会静默跳过。",
    "type": "bool",
    "default": true
  },
  "experimental_interaction_bridge": {
    "description": "启用 QQ Official Interaction 兼容桥",
    "hint": "启用或关闭后必须完整重启 AstrBot。桥接 interaction intent、回调去重和 ACK；不修改磁盘中的 AstrBot 源码。若当前 AppID 无 Interaction Intent 权限，请关闭后重启。",
    "type": "bool",
    "default": false
  },
  "empty_mention_opens_panel": {
    "description": "空 @ 默认发送 Hub 面板",
    "hint": "开启后，在 QQ 官方群里只 @ 机器人且不输入其他内容时，直接发送当前群配置的 Hub 面板；关闭后保留文字提示“请@我输入 /qqhub 查看功能”。斜杠指令不受影响。",
    "type": "bool",
    "default": true
  },
  "operator_openids": {
    "description": "Hub 操作员 OpenID",
    "hint": "用于 permission=operator 的按钮后端校验；多个用逗号、空格或换行分隔。",
    "type": "text",
    "default": ""
  },
  "require_known_clicker": {
    "description": "仅允许「说过话」的用户点击按钮",
    "type": "bool",
    "default": true,
    "hint": "QQ 的按钮回调事件只带乱码 OpenID、不带昵称，因此陌生人无法被识别。开启后，未曾 @ 过机器人（或引用机器人消息）的人点击按钮会被拒绝，用于防止恶意刷卡卡群。真实参与者只需说过一次话即可，不影响正常使用。"
  },
  "show_clicker_name": {
    "description": "卡片顶部显示操作者昵称",
    "type": "bool",
    "default": true,
    "hint": "在卡片最顶部加一行「👤 昵称」，表明这张卡属于谁 / 是谁按的。昵称取自该用户最近一次与机器人说话时的资料，改名后会自动更新；这是纯文本，不是蓝色 @ 超链接（QQ 群主动消息路径会把 At 标签原样显示）。"
  }
}
