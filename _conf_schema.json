{
  "callback_ttl_hours": {
    "description": "后台回调按钮有效期（小时）",
    "hint": "卡片保存新版本后，旧版本后台按钮立即失效；到期按钮返回重复/失效状态。",
    "type": "int",
    "default": 24
  },
  "experimental_interaction_bridge": {
    "description": "启用 QQ Official Interaction 兼容桥",
    "hint": "启用或关闭后必须完整重启 AstrBot。桥接 interaction intent、回调去重和 ACK；不修改磁盘中的 AstrBot 源码。若当前 AppID 无 Interaction Intent 权限，请关闭后重启。",
    "type": "bool",
    "default": false
  },
  "empty_mention_opens_panel": {
    "description": "空 @ 默认发送 Hub 面板",
    "hint": "开启后，在 QQ 官方群里只 @ 机器人且不输入其他内容时，直接发送当前群配置的 Hub 面板；关闭后保留文字提示“请@我输入 /qqhub 查看功能”。斜杠指令不受影响。",
    "type": "bool",
    "default": true
  },
  "operator_openids": {
    "description": "Hub 操作员 OpenID",
    "hint": "用于 permission=operator 的按钮后端校验；多个用逗号、空格或换行分隔。",
    "type": "text",
    "default": ""
  },
  "push_status_display": {
    "description": "主动消息推送状态指示灯",
    "type": "object",
    "hint": "在卡片 Markdown 中使用占位符 {{push_lamp}}（指示灯）与 {{push_status}}（说明文字）。留空则用默认值。状态来自 QQ 授权事件，以及主动推送的实际成败推断；从未收到过任何信号时显示“未知”，不会误报为未开启。",
    "items": {
      "lamp_granted": {
        "description": "已开启 指示灯",
        "type": "string",
        "default": "🟢"
      },
      "lamp_revoked": {
        "description": "未开启 指示灯",
        "type": "string",
        "default": "🔴"
      },
      "lamp_unknown": {
        "description": "未知 指示灯",
        "type": "string",
        "default": "⚪"
      },
      "text_granted": {
        "description": "已开启 说明文字",
        "type": "string",
        "default": "当前群已开启主动消息推送功能"
      },
      "text_revoked": {
        "description": "未开启 说明文字",
        "type": "string",
        "default": "当前群未开启主动消息推送功能"
      },
      "text_unknown": {
        "description": "未知 说明文字",
        "type": "string",
        "default": "当前群主动消息推送状态未知"
      }
    }
  }
}